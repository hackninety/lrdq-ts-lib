# lrdq-ts-lib · 语料库扩建 Todo

> **定位变更**：本库自 v0.2.0 的《六壬大全》单书库，升格为 **大六壬典籍语料库**。
> `detectBifa` 毕法引擎保留（《畢法賦》本即大全语料），`lrdq-ts-lib/docs` 子入口
> 聚合多部公有领域大六壬典籍；宿主 react-liuren 典籍抽屉多书并册。
>
> 调研结论：GitHub 无专门六壬语料库 repo（高星项目全是排盘引擎/app），原文须自采于
> 通用古籍数字化源。故"逐书手工典籍化"是当前唯一可行路径，本库即承此角色。

---

## 0. 基线（已完成，勿重复造）

- **语料**：《六壬大全》四庫全書本 13 篇（序 + 卷一~卷十二），《畢法賦》百法深度结构化
  （`bifa.json`，含 204 附属格，底本笔误按序归正记 `textualIssues`）
- **引擎**：`detectBifa` **97/100 法**（v0.3.0；exact/approx 分级；第 93/97/98 为起传校勘/
  断法方法论不设机判，导出 `UNDETECTABLE_NOS`；可选 `nianMing` 输入启用年命类法）
- **管线**：`docs/book/text/*.txt`（ctext res=260435 转录）→ `scripts/gen-data.mjs`
  → `src/data/{bifa,docs}.json` → `scripts/build.mjs`（tsc + `fixEsmImports`）→ `lrdq-ts-lib/docs` 子入口
- **manifest 项**：`DocMeta = {path, title, group}`，`group ∈ {book, algorithm}`
- **原件存档**：`docs/book/{raw,text}`

---

## 0.5 大全深度结构化（課經七百二十课、神煞表等；当前为整卷典籍化）

> 同书内深化，不受「升格重构」阻塞。红线照旧：不臆断重构、笔误记 `textualIssues` 不静默改字。

| # | 内容 | 产物 | 状态 |
|---|---|---|---|
| 1 | **課經六十四课体**（卷七~卷十，70 节，含卷九「三交課」重出两节照收） | `keju.json` + `lrdq-ts-lib/keju` 子入口（`getKeJing`/`getKeJingEntry`，繁简、剋/克折叠匹配）；宿主三传面板课体/课体细分深链原文 | ✅ v0.4.0 |
| 2 | **神煞三表**（卷一：歲神煞·年支 12 值／十天干神煞·日干 10 值／十二地支神煞·日支 12 值） | `shensha.json` + `lrdq-ts-lib/shensha` 子入口（映射 + 规则条 + 尾注；表头「戊**巳**庚」归正为「己」记 textualIssues） | ✅ v0.4.0 |
| 3 | **月令杂列神煞**（卷一无分隔符混排块，含断行、孤行） | 严格「2 字名 + 12 支连串」才提为按月映射，其余整行列存为规则条（断行按「…在$」启发式拼接） | ✅ v0.4.0（保守解析） |
| 4 | 逐月神煞（正月~十二月立成，每月十二宫上/下栏） | `shensha.json.monthly` 144 宫 1602 处落位（`monthlyAt`/`findMonthlyShenSha`）；生氣/天喜/驛馬与公式及杂列互证；八月「生氣」落午（当在未）等底本偏差照录记校记 | ✅ v0.5.0 |
| 5 | **總鈐**（卷一后半 ~千行，六十甲子七百二十课式盘图，单字盘式行） | **列存不重构**——盘式行无法无损反演，重构违反红线；程序化 720 课式应由排盘引擎生成而非从版面 OCR 反推 | 🚫 按红线搁置 |
| 6 | 課經与毕法互链 | 第99法课名正则补課經名「軒蓋」；同步守卫测试锁第99/100法课名 ⊆ 課經节名；宿主课体深链（KeJingRefs）即运行时互链 | ✅ v0.5.0 |

---

## 1. 升格重构（✅ v0.6.0 完成 —— 第二部书可直接进场）

| # | 改动 | 位置 | 状态 |
|---|---|---|---|
| 1 | 目录分书 | 大全已迁 `docs/corpus/lrdq/{raw,text}`（git mv 保留历史） | ✅ |
| 2 | **manifest 加 `book`** | `DocMeta = {path, title, group, book, dynasty?, author?}`；path 带 slug 前缀（`lrdq/book/juan01.md`） | ✅ |
| 3 | gen-data 书注册表 | `CORPUS = [{slug, book, dynasty, textDir, provenance, furnitureRe, titleRe, titleH1, sectionRes, pages}]` 循环产出；毕法/課經/神煞为大全专属深度分支 | ✅ |
| 4 | 前端按书分组 | TypikonDrawer 分组读 `manifest[].book`（无字段的库用 SOURCES `bookFallback`）；`key = ${lib}:${path}` 不变 | ✅ |
| 5 | **按书分包** | `docs.ts` = manifest 轻入口（同步）+ `getDocMarkdown` **异步**按 slug 路由缓存；载荷 `src/books/<slug>.ts` 动态导入成独立 chunk（fixEsmImports 已支持动态导入补扩展名） | ✅ |
| 6 | 元信息 | description/keywords 改语料库口径；`lrdq` 包名沿用作 id | ✅ |

新书落库只需：`docs/corpus/<slug>/` 存底本 → `CORPUS` 登记（titleRe/sectionRes 等）→
`src/books/<slug>.ts` 载荷包装 + `docs.ts` LOADERS 登记 → 测试与 README 篇目表。

**开放决策 —— 毕法引擎去留**：建议**留在本库**（毕法赋即大全语料，`detectBifa` 与
`getBifaEntry` 共用 `bifa.json`，且 react-liuren `src/plugins/bifa.ts` 已依赖）。若坚持"纯语料库"，
可另拆 `lrdq-bifa` 引擎包——但当前无必要。

---

## 2. 收录清单（优先级 × 与现有功能协同度）

标注：✅ 已有　📝 你已点名　➕ 建议新增

### 🔴 T1 · 核心底座 / 高协同（先收）

| 典籍 | 时代 · 作者 | 收录理由 / 协同点 | 状态 | 底本来源 |
|---|---|---|---|---|
| **六壬心镜** | 唐 · 徐道符 | 现存最早成体系专著，古法总纲；与 zslj《占事略決》成"古法双璧" | ✅ v0.7.0（slug=lrxj，序+卷一~八整卷典籍化，ctext res=486357 清程樹勛手錄本） | ctext |
| ~~大六壬探原~~ | 民国 · 袁树珊 | ~~结构最清晰的入门体系书~~ | ❌ 划掉（2026-07-06：无纯文字底本，OCR 不准，不搞） | — |
| ~~毕法案录~~ | 清 · 纪大奎 | ~~每条毕法配占例，与 detectBifa 深链协同最高~~ | ❌ 划掉（2026-07-06：无纯文字底本，OCR 不准，不搞） | — |
| **大六壬指南（壬归）** | 明末清初 · 陈公献 | 占验派最重要，分类占例极多 | 🚧 底本搁置：ctext 仅有 res=516644《六壬指南注解》＝张洪 2000 年编注本（自序明言"簡注之，僭參己意，並引實例"），陈注与今注混排**无分离标记**，触版权红线不可收；待寻清刊原文转录（维基文库/殆知阁） | 待寻 |

### 🟠 T2 · 占例 / 占验集（丰富断辞素材）

| 典籍 | 时代 · 作者 | 说明 | 状态 |
|---|---|---|---|
| **六壬断案** | 题宋 · 邵彦和 | 断例鼻祖，医案式占例 | 📝 |
| **六壬集应钤** | 明（题佚名） | 大型分类占例钤，断辞素材库（体量大，按类分节） | ➕ |
| **大六壬类阐** | 清 · 张官德 | 义理 + 占例并重 | ➕ |
| **六壬视斯（眎斯）** | 清 · 叶悔亭 | 占验 | ➕ |
| **大六壬占验** | 清抄本 | 占例集 | 📝 |
| **壬占汇选** | 清辑 | 分类占例 | 📝 |

### 🟢 T3 · 歌赋 / 义理 / 注疏

| 典籍 | 时代 · 作者 | 状态 |
|---|---|---|
| **六壬粹言** | 清 · 刘赤江辑 | 📝 |
| **六壬直指** | 清（一说陈公献别本） | 📝 |
| **六壬说约** | 清 | 📝 |
| **六壬神定经** | 宋（景祐本 · 题邵彦和） | 📝 |
| 六壬辨疑 / 六壬统宗 / 六壬明镜 | 明清 | ➕（备选） |

### ⚪ 自成体系（单列，勿并入大六壬册）

| 典籍 | 说明 |
|---|---|
| **六壬神课金口诀** | 明流行（题孙膑）。react-liuren 已有独立金口诀引擎（`src/engines/jinkoujue`）；若收原文须**单列一"书"并标注"非月将加时体系"**，避免与大六壬盘读者混淆 |
| **占事略決** | ✅ 已在 `zslj-ts-lib`（带唐/敦煌古法引擎，**保持独立库**，与本库同形并册） |

---

## 3. 每部书落库模板（照此逐书执行）

1. **定底本**：优先维基文库（已校）→ ctext → 书格/国学大师扫描（需 OCR + 校对）；
   记录 res/url 与版本到该书 `raw/` 出处说明
2. **转录**：整书原件存 `docs/corpus/<slug>/raw`（HTML/图），提取稿存 `text/*.txt`（按卷/章分文件，命名 `NN-<节>.txt`）
3. **配置**：在 `gen-data.mjs` 的 `CORPUS` 注册该书 `{slug, book, titleRe, sectionRes, provenance, level:'整卷'|'深结构'}`
4. **生成**：`npm run gen`，核对 manifest 篇数 / 小节数；转录笔误按序位归正并记 `textualIssues`，**不臆改原字**
5. **校对**：繁体保留、繁简不混；底本版面（盘式图、神煞表行）保持原行原序，不重构；通读一遍
6. **验证**：`npm test`；前端抽屉核对 `book` 分组与站内 `.md` 链接跳转
7. **发版**：bump version，更新 `README.md` 篇目表 + `INTEGRATION.md`，打 tag；react-liuren 依赖切新 tag

---

## 4. 数据源与版权

- **公有领域截止**：清及以前无虞；民国印本须作者殁逾 50 年（袁树珊 1881–1968，已过，可收）
- **首选源**：维基文库 `zh.wikisource.org`（校对最好）、`ctext.org`（结构化）；
  扫描源 书格 `shuge.org` / 国学大师 `guoxuedashi.net`（需 OCR 校对）；殆知阁语料做粗校打底
- **🚫 勿收**：近现代仍在版权期著作（徐伟刚《大六壬预测学》等）
- 每书 `raw/` 保留出处 url/res，便于复核与再校

---

## 5. 质量红线（承 gen-data 现口径）

- 底本不臆断重构：盘式图、神煞表行保持原行原序
- 转录笔误按序位归正 + 记 `textualIssues`，**不静默改字**
- 繁体原文，繁简统一不混用
- 结构化分级：占例集 / 歌赋只做**整卷典籍化**（高置信排版：卷题→H1、独行小节题→H2、页衬行清理）；
  仅《畢法賦》《課經》、神煞表一类强结构文本做**深度结构化**（盘式版面如總鈐永远列存）
